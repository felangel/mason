name: mustachex
description: Mustache Extended. A fork from mustache and mustache_template that will be maintained and that has improvements over it.... or just "Mustache with steroids"
version: 1.1.0
repository: https://gitlab.com/Rodsevich/mustachex
# homepage: https://www.example.com
environment:
  sdk: ">=2.12.0 <4.0.0"

dependencies:
  mustache_recase: '>=2.4.0 <4.0.0'
  path: ^1.8.0
  quiver: ^3.0.1
  recase: ^4.0.0
  collection: ^1.16.0

dev_dependencies:
  test: ^1.17.5

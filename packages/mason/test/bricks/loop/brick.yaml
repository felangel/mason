name: loop
description: A new brick created with the Mason CLI.

version: 0.1.0+1

environment:
  mason: ">=0.1.0-dev <0.1.0"

vars:
  values:
    type: array
    values:
      - development
      - staging
      - production

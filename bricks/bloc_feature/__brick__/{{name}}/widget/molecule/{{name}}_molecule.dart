import 'package:flutter/material.dart';

class {{name.pascalCase()}}Molecule extends StatelessWidget {
  const {{name.pascalCase()}}Molecule({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
import 'package:flutter/material.dart';

class {{name.pascalCase()}}Atom extends StatelessWidget {
  const {{name.pascalCase()}}Atom({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}